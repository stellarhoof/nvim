{
  "experiment": {
    "body": [
      "<SlotsContext.Provider value={slots}>",
      "  <TabsPrimitive",
      "    ref={ref}",
      "    {...props}",
      "    className={twMerge(slots.root(), props.className)}",
      "  />",
      "</SlotsContext.Provider>"
    ],
    "prefix": "experiment"
  },
  "listprops": {
    "body": [
      "export interface ListProps extends TabListPrimitiveProps<any> {",
      "  className?: string",
      "}"
    ],
    "prefix": "listprops"
  }
}
